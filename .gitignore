# Ignorar archivos generados por Maven (opcional pero recomendado)
*.log
*.class
# Ignorar archivos de configuración de VS Code (opcional si no quieres compartirlos)
# Si deseas subir .vscode/settings.json, no incluyas la línea de abajo
#.vscode/